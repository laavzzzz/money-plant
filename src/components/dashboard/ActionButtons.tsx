export default function ActionButtons() {
  return (
    <div className="grid grid-cols-3 gap-3">

      <button className="glass-card py-3 text-sm">
        + Add
      </button>

      <button className="glass-card py-3 text-sm">
        Goals
      </button>

      <button className="glass-card py-3 text-sm">
        AI
      </button>

    </div>
  );
}